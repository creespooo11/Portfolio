# Infraestructura local

Esta carpeta queda reservada para configuraciones de despliegue, scripts de inicialización y documentación operativa.

La primera versión de la infraestructura se define en el `docker-compose.yml` de la raíz para que el stack pueda arrancar antes de que existan los servicios de aplicación.
